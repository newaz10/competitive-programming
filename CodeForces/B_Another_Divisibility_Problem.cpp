#include <bits/stdc++.h>
using namespace std;

void solve() {
    long long x;
    cin >> x;
    
    cout << 2*x << "\n";
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int t;
    cin >> t;

    while (t--) {
        solve();
    }
    
    return 0;
}